<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.admin.zivima">

    <!-- <uses-permission android:name="android.permission."/> -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main2Activity" />
        <activity android:name=".Main3Activity" />
        <activity android:name=".Main4Activity" />
        <activity android:name=".FarmerWelfare" />
        <activity android:name=".Health" />
        <activity android:name=".Ministrieslist" />
        <activity android:name=".Education" />
        <activity android:name=".AdminLogin" />
        <activity android:name=".ViewUploadsActivity" />
        <activity android:name=".ListingAdapter" />
        <activity android:name=".ShowUsers"></activity>
    </application>

</manifest>